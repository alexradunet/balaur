# Widget, element, and state model

The learner can trace the Balaur theme update through a callback, `State`, `setState`, the associated `StatefulElement`, and `build`. The learner also understands that widget configuration supplies dependencies to `State` through the element-managed `widget` property. Future lessons can use this model when they introduce lifecycle, state ownership, and application state.
