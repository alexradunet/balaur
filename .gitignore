# Private data — never commit (AGENTS.md "Safety")
pb_data/
*.db
*.db-shm
*.db-wal

# Models and inference libraries (downloaded data, not source)
*.gguf
models/
libs/

# Build artifacts
/balaur
/balaur.exe
dist/
.cache/
tmp/

# Local environment
.env
.env.*

# Claude Code local state (settings.local.json, locks, worktrees)
.claude/
