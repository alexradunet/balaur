---
phase: review
status: done
ticket: 03
date: 2026-07-25
verdict: approved
---

# Standards review: ticket 03 — browser-check driver rework

## Verdict: APPROVED

## Findings
- [judgement] `.pi/skills/browser-check/SKILL.md:9` — doc drift (AGENTS.md §14): the node‑check limitation sentence still names "IndexedDB" as the canonical example of behavior `node --check` cannot prove ("`node --check` alone never proves persistence, IndexedDB, or canvas behavior"). The app is now folder‑vault/OPFS‑backed (ADR‑0004; IndexedDB deleted in ticket 02). The claim is not false (a syntax check cannot prove IndexedDB behavior in general), but the example is anachronistic after a change whose entire purpose was to purge IndexedDB. Reword to "OPFS persistence" or "folder‑vault persistence". Optional polish; does not block.

## Summary

One judgement‑level doc‑wording nit (SKILL.md:9 stale "IndexedDB" example); no hard violations. The diff is correct, fully matches plan §4 and spec seam 1, and all behavioral verification I ran myself passes: `node --check` exit 0; `contract` 16/16 exit 0; `smoke --offline` 14/14 exit 0 (gate record `message:empty button:enabled` captured before the stub, reload re‑pick `7 -> 7`, offline shell from `balaur-shell-v13` cache); `widgets` 26/26 exit 0; `git diff --check ca139f1..42d0f84` clean.

### Verified against the assigned correctness points

1. **`bootPastLanding`** (`browser-check.mjs:251-266`): waits for `#openVaultFolder` present + enabled + `window.balaurVaultReady !== undefined` (deviation D1), captures the pre‑stub gate state (`messageEmpty`, `buttonEnabled`) at `:259` *before* installing the stub at `:260`, installs `window.showDirectoryPicker = async () => (await navigator.storage.getDirectory()).getDirectoryHandle(<subdirectory>, { create: true })`, computes the click point via `getBoundingClientRect()` center with `Math.round` (`:262`), performs a real `session.click(x, y)` (not `el.click()`), then waits for `window.balaurCanvas && document.querySelectorAll('.canvas-node').length > 0`. Applied at all 8 balaurCanvas‑boot sites: smoke prelude (`:284`), smoke reload (`:420`), smoke offline (`:430`), components prelude (`:464`), components reload (`:662`), failureSession boot (`:1240`), failureSession reload (`:1377`), shot (`:1924`). The only remaining `waitFor("window.balaurCanvas && document.querySelectorAll('.canvas-node')...")` is inside the helper itself (`:264`); all other balaurCanvas waits are post‑boot assertion conditions (`getCurrentCanvas().id`, `getDocument().nodes.length > before`, `getDocument().nodes.some(...)`) and correctly stay. The `eval` subcommand (`:1907-1916`) stays raw (navigate → optional `--wait` → evaluate; no `bootPastLanding`).
2. **Binding constraint**: `grep showDirectoryPicker app.js` → only `app.js:326` (incompatibility gate: `typeof window.showDirectoryPicker!=="function"`) and `app.js:346` (pick handler: `handle=await window.showDirectoryPicker({mode:"readwrite"})`) with the explicit "Never capture showDirectoryPicker in a module‑scope const" provenance comment at `:343-345`. No module‑scope capture exists (`grep "showDirectoryPicker =" app.js` → none). The stub is honored by the real click.
3. **`contract` subcommand** (`CONTRACT_EVAL :1732`, runner `contract() :1857`, CLI dispatch `:1901`, usage comment `:6`, Unknown‑command line `:1929`): dynamically imports `/storage/directory-vault.js`, creates fresh per‑run OPFS subdirectories (`contract-main-<ts>`, `contract-restore-<ts>`, `contract-journal-<ts>`), and asserts the full spec seam‑1 surface. Cross‑checked against `storage/directory-vault.js`: `write` returns `{path,mediaType,size,hash,modifiedAt,revision}`; `stat` meta has `path/mediaType/size/hash/modifiedAt/revision`; `move` returns `{path:to, ..., hash}` and throws `WRITE_CONFLICT` on destination exists (`:233`); `remove` returns `true` and honors `expectedHash` precondition (`:212`); `snapshot` returns `{format:"balaur-vault-snapshot",...}` (`:267`); `restore` returns `{revision,count}` (`:300`); `changesSince` entries carry `revision`/`operation` with ops create/modify/move/remove (`_bump :37-41`). Every spec seam‑1 behavior is asserted: create/read; stat meta shape; exists true/false; list ordering + prefix filter; three expectedHash outcomes (null create‑conflict → `WRITE_CONFLICT`, mismatch with `details.expected`/`details.actual`, correct succeeds); `NOT_FOUND` on read; `PATH_CASE_COLLISION` on create; move success + destination conflict; remove success + precondition mismatch; snapshot/restore round‑trip into a second fresh subdirectory; changesSince journal ordering (4 entries, strictly increasing revisions, ops present, plus an extra `changesSince(journal[1].revision).length === 2` correctness check); `TypeMismatchError` → `PATH_COMPONENT` (write onto a directory name, mapped to `PathError` at `directory-vault.js:84`). Returns `{ok, failures, checks}`; runner splits `checks` into PASS/FAIL records, adds a console‑error record if any, and exits nonzero on failure (`contract() :1883-1889`).
4. **smoke rework** (`:284-285`, `:401`, `:420`, `:430`): gate record `message:empty button:enabled` asserted from pre‑stub state; assertions 1‑8 unchanged after the prelude; step 9 reload calls `bootPastLanding(session)` again (re‑stub + re‑pick the same `vault-smoke` subdirectory) then asserts title/node count (`7 -> 7`); step 10 `--offline` does `setOffline(true)` → `reload` → `bootPastLanding` → assert `!!document.querySelector('.canvas') && !!window.balaurCanvas` → `setOffline(false)`, and the shell renders from `balaur-shell-v13` cache.
5. **Deviations**: D1 (also wait on `window.balaurVaultReady`) is sound and documented. Verified the app wiring: `bootCanvasApp()` (`app.js:324`) is async; its first `await` is `new Promise(resolve => { $("#openVaultFolder").onclick = ... })` (`:336`), whose executor runs synchronously and wires the handler before `bootCanvasApp()` returns its promise; `window.balaurVaultReady = vaultReady` is assigned at `app.js:1978` immediately after, so `balaurVaultReady !== undefined` is a correct proxy for "the pick handler is wired." The failure‑session race (register.js blocked widening the gate‑to‑handler window) is real and the fix is driver‑only; the gate record still asserts button enabled + empty message captured before the stub, so the gate assertion is not weakened. D2 (poll for the new note node instead of a fixed 500ms sleep, `:401`) is sound: notes are async file‑backed per ADR‑0004, the 5s ceiling with `.catch(() => {})` falls through to a graceful FAIL, and the assertion logic is unchanged. D1 is documented in SKILL.md "Vault gate and the picker stub" step 1; D2 is a step‑7 timing detail documented in the code comment (`:396-398`) and the implement artifact, which is the right place (plan §4.4 does not require it in SKILL.md).
6. **SKILL.md**: documents the `contract` subcommand (Commands block `:48`, dedicated section `:123`); the picker‑stub mechanism and real CDP click (`:90` section); re‑pick‑after‑reload (`:99-103`); `--profile` persistence via the same OPFS subdirectory (`:101`); `eval` stays raw (`:104`); and manual‑smoke boundaries (real picker, external‑change reconciliation, Firefox/Safari gate, `:106-108`). No stale IndexedDB‑era claims survive except the one judgement finding at `:9`. The `components` inspector probe timeout (F1 in the implement artifact, `browser-check.mjs:858`) is pre‑existing, downstream of the two boot sites this ticket owns, untouched by this diff, and correctly reported as out of scope for a follow‑up; not a finding against ticket 03.
